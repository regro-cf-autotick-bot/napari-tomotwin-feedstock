{% set name = "napari-tomotwin" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari-tomotwin-{{ version }}.tar.gz
  sha256: c0745b846986219a329fd006ebb0e4eaee08fb5ca81e0dacf5a257e1fd7ff4ab

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.10
    - setuptools-scm
  run:
    - magicgui
    - matplotlib-base
    # - mrcfile  # Used in tests only
    - napari
    - napari-clusters-plotter >=0.7.2
    - numpy
    - pandas
    - python >=3.10
    - scipy

test:
  imports:
    - napari_tomotwin
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/napari-tomotwin
  summary: Several tools for the work with TomoTwin
  license: MPL-2.0
  license_file: LICENSE
  dev_url: https://github.com/MPI-Dortmund/napari-tomotwin

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
    - thorstenwagner
